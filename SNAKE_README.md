# AI Snake Lab —— 智能贪吃蛇实验室

一个使用 AI 编程智能体完成的贪吃蛇课程作业项目。在普通贪吃蛇的基础上，增加了基于 **BFS 广度优先搜索** 的 AI 自动寻路模式，并结合贪心 + 生存策略提升 AI 的实际游戏表现。

---

## 项目简介

「AI Snake Lab」是一个纯前端、零依赖的贪吃蛇游戏。打开 `index.html` 即可运行，无需任何后端、数据库或构建工具。

项目同时支持：
- **手动模式**：玩家用方向键 / WASD 控制蛇移动。
- **AI 自动模式**：蛇使用 BFS 算法自动寻找食物路径，并在路径被堵时使用贪心生存策略尽量存活。
- **模式对比**：分别记录手动与 AI 的最高分、游戏局数，可直观对比两种模式的表现。

## 项目目标

1. 用 AI 编程智能体完成一个完整、稳定的贪吃蛇程序。
2. 在普通贪吃蛇基础上，加入有创意的 AI 自动模式。
3. AI 至少使用 BFS 广度优先搜索实现自动寻路，并结合贪心策略。
4. 支持手动玩与观看 AI 自动玩。
5. 能够比较手动模式与 AI 自动模式的得分。
6. 代码结构、注释、功能完整度和可读性适合上传到 GitHub / Gitee 展示。

## 技术栈

- HTML5
- CSS3
- 原生 JavaScript（ES6+）
- HTML5 Canvas 2D
- Web Audio API（音效，无外部资源）
- localStorage（数据持久化）

> 不使用 React / Vue / jQuery 等框架，不使用第三方游戏引擎，不依赖任何外部 npm 包。

## 功能介绍

### 普通模式

- 方向键 `↑ ↓ ← →` 或 `W A S D` 控制蛇移动。
- 吃到食物蛇身增长、分数增加，并重新生成食物。
- 撞墙或撞到自身则游戏结束，显示 Game Over 遮罩。
- 支持暂停 / 继续、重新开始、开始前准备状态。
- 防止连续快速按键导致蛇立即反向撞自己。
- 食物不会生成在蛇身上。
- 游戏状态明确区分：`READY / PLAYING / PAUSED / GAME_OVER`，AI 模式以独立标记 `aiEnabled` 与播放状态并存。

### AI 自动模式

- 点击「🤖 进入 AI 自动模式」按钮或按 `A` 键切换。
- AI 开启后玩家不再控制方向，蛇自动寻找食物。
- 右侧面板实时显示当前算法、状态、路径长度、搜索节点数、当前模式。
- Canvas 上可显示 AI 计算出的 BFS 路径（半透明蓝色圆点），可用 `L` 键或开关显示 / 隐藏。

### BFS 自动寻路

代码中标注 `BFS PATHFINDING`。核心逻辑：

1. 将 25×25 棋盘抽象为二维网格，每格是一个节点。
2. 蛇头作为 BFS 起点，食物作为目标节点。
3. 蛇身（除尾巴，因为尾巴本 tick 会移走）和棋盘边界视为障碍。
4. 使用数组队列实现 BFS，`Uint8Array visited` 防重复访问，`Int32Array parent` 记录前驱。
5. 找到食物后通过 `parent` 数组回溯出完整路径。
6. AI 选择路径上的下一格作为下一步方向。

### 贪心策略

当 BFS 找不到直达食物的路径时，进入贪心 / 生存 fallback：

1. 枚举所有合法方向（不撞墙、不撞蛇身，忽略尾巴）。
2. 对每个方向计算：
   - 与食物的 **Manhattan 距离**（越小越好）。
   - 从该位置出发的 **可达空间大小**（flood fill，越大越安全）。
3. 评分公式：`score = 可达空间 * 10 - 曼哈顿距离`，优先安全，再接近食物。
4. 选择得分最高的方向。

### AI 生存策略

整体决策流程：

```
BFS 寻找食物
      ↓
  找到路径？
   ↓ 是              ↓ 否
沿 BFS 路径       贪心 / 生存策略
                    ↓
              选择安全方向
```

- 不保证 AI 永远不死，但通过合理策略尽可能延长生存时间。
- 当完全没有合法移动时，AI 会保持原方向，最终自然结束游戏。

### 手动模式与 AI 模式对比

界面中提供两组统计卡片：

| 统计项 | 说明 |
|--------|------|
| 当前得分 | 本局实时分数 |
| 手动最高分 | 手动模式历史最高分 |
| AI 最高分 | AI 模式历史最高分 |
| 手动局数 / AI 局数 | 两种模式的累计游戏次数 |
| 蛇长度 / 游戏时间 | 当前局数据 |

数据通过 `localStorage` 持久化，刷新页面后仍保留。**不预设 AI 一定比玩家厉害**，实际数据以运行结果为准。

### 游戏难度

- Easy / Normal / Hard 三档速度，通过下拉选择。
- 吃到食物后速度会轻微提升，但设有 `MIN_SPEED` 上限，不会无限加快。
- 修改速度不破坏当前游戏状态。

### 更多功能

- 开始 / 暂停 / 重开按钮
- 手动 / AI 模式切换
- 难度选择、音效开关、AI 路径开关
- 最高分记录（手动 + AI）
- 当前分数、蛇长度、游戏时间、局数统计
- 当前模式、当前算法、AI 状态显示
- Game Over 遮罩
- 多类型食物（🍎🍓🥑🍞🍌🍒，均 +10）与奖励食物（★，+20 分），全部 Canvas 原生绘制
- Light / Dark 双主题，Canvas 同步变色
- 完整快捷键支持

### 可爱化小蛇角色

蛇不再画成方块，而是圆润拟人化的可爱角色：

- **圆头 + 圆身**：蛇头与蛇身均为圆形，蛇身带肚皮高光。
- **眼睛**：眼白 + 瞳孔 + 高光，瞳孔朝移动方向偏移；AI 模式下瞳孔更靠前（专注表情）。
- **腮红**：头两侧半透明粉色小圆。
- **嘴巴**：朝前方的小弧线；Game Over 时变成闭眼 + 嘴角下弧（伤心表情）。
- **朝向**：眼睛 / 嘴巴 / 饰品随 `dir`（up/right/down/left）整体旋转。
- **呼吸动画**：蛇身随 `Math.sin` 相位轻微缩放，不创建新对象。
- **吃食物反馈**：蛇头放大脉冲（0.18 倍，约 0.3s 回弹）+ 爱心 / 星星粒子（轻量数组，~1s 自动清除）。
- **图案**：不同皮肤可在蛇身画草莓籽、科技电路等图案。

### 装扮系统（Snake Closet）

点击右侧「🎀 蛇蛇衣橱」按钮或按 `C` 键打开轻量装扮面板：

- **预览 Canvas**：实时展示当前选中皮肤 + 饰品组合。
- **皮肤网格**：点击切换预览，未解锁项显示 🔒 与解锁条件。
- **饰品网格**：同上。
- **Apply**：应用预览到游戏；**Default**：恢复 Classic + 无饰品。
- 装扮**仅改变视觉**，不影响 BFS / 碰撞 / 分数等任何游戏算法。

#### 皮肤 Skins

| 皮肤 | 风格 | 解锁条件 |
|------|------|----------|
| 🌿 Classic | 清新绿 | 默认 |
| 🍓 Strawberry | 红粉水果 + 草莓籽图案 | 单局得分 ≥ 50 |
| 🍋 Lemon | 柠檬黄 | 默认 |
| 🌱 Mint | 薄荷绿 | 默认 |
| 🫐 Blueberry | 蓝紫色 | 默认 |
| 🤖 Tech | 青色 + 电路图案 + 科技眼 | AI 模式累计吃 20 个食物 |

#### 饰品 Accessories

| 饰品 | 解锁条件 |
|------|----------|
| ⬜ 无 | 默认 |
| 👑 小王冠 | 单局得分 ≥ 100 |
| 🎩 小帽子 | 默认 |
| 🎀 小领结 | 默认 |
| 🌸 小花朵 | 累计游戏 5 局 |
| 🧣 小围巾 | 默认 |

所有饰品用 Canvas 原生图形（圆 / 弧 / 路径）绘制，无外部图片资源。

### 解锁系统

解锁条件仅作为游戏内玩法展示，**不涉及真实付费**：

- 得分达到 50 → 解锁 🍓 草莓皮肤
- 得分达到 100 → 解锁 👑 小王冠
- 累计游戏 5 局 → 解锁 🌸 小花朵
- AI 模式累计吃 20 个食物 → 解锁 🤖 科技蛇

解锁状态通过 `localStorage` 持久化，刷新页面后保留。新解锁时弹出顶部 Toast 提示并播放解锁音效。

### Current Snake 个性化

右侧顶部「Current Snake」卡片：

- **头像 Canvas**：实时绘制当前蛇头（含饰品），随主题变色。
- **蛇名输入框**：玩家可自由命名（最长 12 字符），默认「小绿」，保存到 `localStorage`。
- **Skin / Accessory / Mode** 实时显示。

### 视觉与逻辑解耦

为方便后续扩展，三个系统严格分离：

- **Snake 数据结构**：`snake[]` 只存 `{x, y}` 坐标；`dir` / `nextDir` / `score` / `length`。
- **Skin 数据结构**：`SKINS[id] = { bodyColor, headColor, bellyColor, eyeStyle, pattern, accent }；ACCESSORIES[id] = { id, name }`。仅由 `render()` 读取。
- **AI 系统**：`findPathBFS` / `chooseSurvivalMove` / `updateAI` 只读 `snake` 与 `food` 坐标。

修改蛇的外观不会影响 BFS 与游戏逻辑，反之亦然。

### 性能优化

1. Canvas 只创建一次，通过 `devicePixelRatio` 适配高 DPI 屏幕。
2. 游戏逻辑 `tick` 与渲染 `requestAnimationFrame` 分离：tick 按固定间隔执行，渲染走 RAF。
3. 避免大量 DOM 操作，状态变化时批量更新 UI。
4. 不使用嵌套 `setTimeout`，用单一时间步长循环。
5. BFS 中使用 `Uint8Array` / `Int32Array` 替代普通对象，减少 GC 压力。
6. AI 每 tick 只在必要时重新计算路径（BFS 每次移动重算一次，规模 ≤ 25×25=625 节点）。
7. 食物生成通过收集空格列表实现，避免死循环。
8. `resize` 时重新 fitCanvas，防止模糊。

## 项目结构

```
snake-ai/
├── index.html              # 单文件包含 HTML + CSS + JS（核心程序）
├── README.md               # 项目说明
└── docs/
    └── development-log.md  # 开发日志（Bug 记录）
```

核心 `index.html` 内部分为以下模块（见脚本顶部注释）：

1. Config 配置
2. State 游戏状态
3. Skin 系统（装扮数据，与 game 解耦）
4. 食物类型（多类型 + Canvas 原生绘制）
5. DOM 引用
6. Canvas 高 DPI 适配
7. Theme 主题
8. Sound 音效
9. Collision 碰撞检测
10. Food 食物生成
11. Init 初始化
12. Input 键盘输入
13. BFS 自动寻路
14. Greedy / Survival 策略
15. AI Controller
16. Move / Tick
17. 解锁系统（Unlock）
18. Render 渲染（可爱化蛇头 + 多类型食物 + 粒子）
19. 粒子系统
20. 预览 / 头像渲染
21. UI 同步
22. Toast 解锁提示
23. 装扮衣橱（Closet Modal）
24. Game Loop
25. localStorage
26. 事件绑定
27. Boot 启动

## 操作说明

### 按钮
- **开始**：开始新一局。
- **暂停**：暂停 / 继续。
- **重开**：重新开始当前局。
- **🤖 AI 模式**：切换手动 / AI 自动模式。

### 设置
- **游戏速度**：Easy / Normal / Hard。
- **主题**：Light 清新 / Dark 科技。
- **显示 AI 路径**：在 Canvas 上绘制 BFS 路径。
- **音效**：开关吃食物、游戏结束等音效。

## 快捷键

| 按键 | 功能 |
|------|------|
| `↑ ↓ ← →` 或 `W A S D` | 控制蛇移动（手动模式） |
| `Space` | 开始 / 重开 |
| `P` | 暂停 / 继续 |
| `A` | 切换 AI 自动模式 |
| `L` | 显示 / 隐藏 AI 路径 |
| `C` | 打开蛇蛇衣橱 |

## 运行方法

直接用浏览器打开 `index.html` 即可运行，无需任何安装或服务器。

```bash
# Windows
start index.html

# macOS
open index.html

# Linux
xdg-open index.html
```

## AI 编程流程

本项目使用 AI 编程智能体辅助完成，整体流程如下：

1. **生成基础贪吃蛇**：搭建 Canvas、蛇数据结构、渲染、主循环骨架。
2. **调试键盘控制**：实现方向键 + WASD，处理反向禁止与按键缓冲。
3. **调试碰撞检测**：拆分为 `checkWallCollision` / `checkSelfCollision` / `checkFoodCollision`。
4. **加入食物生成**：收集空格列表随机生成，避免出现在蛇身上，处理棋盘占满。
5. **加入分数系统**：普通食物 +10，奖励食物 +20，localStorage 保存最高分。
6. **增加 AI 自动模式**：`aiEnabled` 标记，AI 模式下禁用手动输入。
7. **使用 BFS 实现寻路**：队列 + visited + parent 回溯，障碍为蛇身 + 边界。
8. **增加贪心和安全策略**：BFS 失败时用可达空间 + 曼哈顿距离评分选择方向。
9. **增加 AI 路径可视化**：Canvas 半透明圆点显示路径，开关控制。
10. **优化 Canvas 渲染**：高 DPI 适配、tick 与 RAF 分离、减少临时对象。
11. **测试手动模式和自动模式**：验证移动、吃食物、碰撞、切换模式。
12. **可爱化小蛇角色**：圆头 + 眼睛 + 腮红 + 嘴巴 + 朝向 + 呼吸 + 吃食物脉冲 + 粒子。
13. **装扮系统**：Skin / Accessory 数据结构与 game 解耦，衣橱 Modal + 预览 Canvas。
14. **解锁系统**：得分 / 局数 / AI 食物数触发，localStorage 持久化，Toast 提示。
15. **多类型食物**：🍎🍓🥑🍞🍌🍒 全部 Canvas 原生绘制。
16. **Current Snake 个性化**：头像 + 蛇名输入 + 装扮 / 模式展示。
17. **修复运行过程中发现的 Bug**：详见 `docs/development-log.md`。

> 开发过程中产生的实际 Bug 与修复记录见 `docs/development-log.md`。
